runtime: python3.11

serve:
  - type: python
    callable: arteriya.wsgi:application
    envs:
      DEBUG: "false"
      ALLOWED_HOSTS: "*.amvera.io,localhost,127.0.0.1"
  - type: static
    source: static
    target: /static
